<% if @success %>
  document.getElementById('results_container').innerHTML = `<%= render partial: 'results_table', locals: { result: @result, page: @page, per_page: @per_page } %>`;
<% else %>
  M.toast({ html: 'An error presented while fetching data' });
<% end %>

document.getElementById('loader').classList.add('hide');